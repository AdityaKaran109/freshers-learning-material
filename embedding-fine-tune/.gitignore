# Prepared training data IS committed (see data/). Everything below is not:
# it is either large, downloadable, or regenerated by the scripts.

# Downloaded model weights (model_download.py)
FremyCompany/
*.safetensors
*.bin

# Downloaded raw dataset (model_download.py re-downloads it in seconds).
# 32 MB, and re-hosting someone else's dataset wholesale is not ours to do.
shudhanshusingh/
mukuldeshantri/

# Trained models and training checkpoints (finetune.py)
models/
checkpoints/
*.log

# Python
venv/
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
